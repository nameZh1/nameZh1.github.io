# Hexo 博客开发指南

本文档介绍如何在本地开发环境中操作 Hexo 博客，包括环境搭建、文章编写和部署流程。

## 目录

- [环境准备](#环境准备)
- [本地开发](#本地开发)
- [文章编写](#文章编写)
- [部署流程](#部署流程)
- [常用命令速查](#常用命令速查)

---

## 环境准备

### 1. 安装 Node.js

确保已安装 Node.js（推荐 v14.x 或更高版本）：

```bash
# 检查 Node.js 版本
node -v

# 检查 npm 版本
npm -v
```

### 2. 安装 pnpm（推荐）

本项目使用 pnpm 作为包管理器：

```bash
# 安装 pnpm
npm install -g pnpm

# 检查 pnpm 版本
pnpm -v
```

### 3. 安装项目依赖

```bash
# 在项目根目录执行
pnpm install
```

### 4. 安装 Hexo CLI（可选）

如果需要全局使用 Hexo 命令：

```bash
npm install -g hexo-cli
```

---

## 本地开发

### 启动本地服务器

```bash
# 使用 pnpm 脚本
pnpm server

# 或直接使用 hexo 命令
hexo server

# 简写
hexo s
```

启动后访问 `http://localhost:4000` 即可预览博客。

### 清理缓存

当遇到显示问题或修改配置后，建议先清理缓存：

```bash
# 使用 pnpm 脚本
pnpm clean

# 或直接使用 hexo 命令
hexo clean
```

### 生成静态文件

```bash
# 使用 pnpm 脚本
pnpm build

# 或直接使用 hexo 命令
hexo generate

# 简写
hexo g
```

生成的静态文件位于 `public/` 目录。

---

## 文章编写

### 创建新文章

```bash
# 创建新文章
hexo new "文章标题"

# 创建草稿
hexo new draft "草稿标题"

# 创建页面
hexo new page "页面名称"
```

新文章会创建在 `source/_posts/` 目录下。

### 文章格式

每篇文章以 Markdown 格式编写，文件开头需要包含 Front-matter：

```markdown
---
title: 文章标题
date: 2025-12-25 10:00:00
tags:
  - 标签1
  - 标签2
categories:
  - 分类名称
thumbnail: 缩略图URL（可选）
---

这里是文章正文内容...
```

### Front-matter 常用字段

| 字段 | 说明 | 示例 |
|------|------|------|
| `title` | 文章标题 | `title: 我的第一篇文章` |
| `date` | 发布日期 | `date: 2025-12-25 10:00:00` |
| `tags` | 标签（可多个） | `tags: [Hexo, 博客]` |
| `categories` | 分类 | `categories: 技术` |
| `thumbnail` | 缩略图 | `thumbnail: /images/cover.jpg` |
| `password` | 文章密码（加密） | `password: 123456` |
| `excerpt` | 文章摘要 | `excerpt: 这是摘要内容` |

### 文章目录结构

```
source/
├── _posts/           # 文章目录
│   ├── hello-world.md
│   ├── 日志2024年9月30日.md
│   └── ...
├── _data/            # 数据文件
│   └── essays.yml
├── aboutMe/          # 关于我页面
│   └── index.md
├── essays/           # 随笔页面
│   └── index.md
├── tags/             # 标签页面
│   └── index.md
└── images/           # 图片资源
    └── ...
```

### 插入图片

1. **使用本地图片**：将图片放入 `source/images/` 目录
   ```markdown
   ![图片描述](/images/example.png)
   ```

2. **使用外部链接**：
   ```markdown
   ![图片描述](https://example.com/image.png)
   ```

---

## 部署流程

### 方式一：GitHub Actions 自动部署（推荐）

本项目已配置 GitHub Actions 自动部署，只需将代码推送到 `master` 分支即可自动部署。

#### GitHub Actions 是什么？

GitHub Actions 是 GitHub 提供的持续集成/持续部署（CI/CD）服务。它可以在特定事件（如代码推送）发生时，自动执行预定义的任务（如构建、测试、部署）。

#### ⚠️ 工作流文件位置（重要！）

**GitHub Actions 的工作流配置文件必须放在 `.github/workflows/` 目录下**：

```
项目根目录/
├── .github/
│   └── workflows/
│       └── deploy.yml    ← ✅ 正确位置！
├── deploy.yml            ← ❌ 错误位置！GitHub 无法识别
├── source/
├── themes/
└── ...
```

> ⚠️ 如果文件放在根目录（如 `./deploy.yml`），GitHub 将**无法识别**该工作流，自动部署不会触发！

#### 工作流配置详解

工作流文件 [`.github/workflows/deploy.yml`](.github/workflows/deploy.yml) 的结构说明：

```yaml
# ============================================================
# 工作流名称 - 显示在 GitHub Actions 页面
# ============================================================
name: Deploy Blog

# ============================================================
# 触发条件
# ============================================================
on:
  push:
    branches:
      - master          # 推送到 master 分支时触发
  workflow_dispatch:    # 允许手动触发

# ============================================================
# 作业定义
# ============================================================
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest  # 使用 Ubuntu 最新版作为运行环境
    
    steps:
      # 步骤 1：检出代码
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true      # 同时拉取子模块（如主题）
          fetch-depth: 0        # 获取完整历史
      
      # 步骤 2：设置 Node.js 环境
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'    # 使用 Node.js 20
      
      # 步骤 3：安装 pnpm
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      
      # 步骤 4：缓存依赖（加速构建）
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
      
      # 步骤 5：安装项目依赖
      - name: Install dependencies
        run: pnpm install
      
      # 步骤 6：构建静态文件
      - name: Build
        run: |
          pnpm clean
          pnpm build
      
      # 步骤 7：部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
```

#### 工作流执行流程图

```
┌─────────────────────────────────────┐
│  代码推送到 master 分支              │
└─────────────────┬───────────────────┘
                  ▼
┌─────────────────────────────────────┐
│  GitHub 检测到 push 事件             │
└─────────────────┬───────────────────┘
                  ▼
┌─────────────────────────────────────┐
│  读取 .github/workflows/deploy.yml  │
└─────────────────┬───────────────────┘
                  ▼
┌─────────────────────────────────────┐
│  创建 Ubuntu 虚拟环境                │
└─────────────────┬───────────────────┘
                  ▼
┌─────────────────────────────────────┐
│  执行各个步骤：                      │
│  1. 检出代码（包含子模块）            │
│  2. 安装 Node.js 20                 │
│  3. 安装 pnpm                       │
│  4. 恢复/缓存依赖                    │
│  5. 安装项目依赖                     │
│  6. hexo clean && hexo generate     │
│  7. 部署 public/ 到 gh-pages 分支   │
└─────────────────┬───────────────────┘
                  ▼
┌─────────────────────────────────────┐
│  GitHub Pages 自动更新网站           │
└─────────────────────────────────────┘
```

#### 使用方法

```bash
# 1. 添加所有更改
git add .

# 2. 提交更改
git commit -m "更新文章"

# 3. 推送到远程仓库（触发自动部署）
git push origin master
```

#### 查看部署状态

1. 打开 GitHub 仓库页面
2. 点击 **Actions** 标签
3. 查看工作流运行状态：
   - ✅ 绿色勾号：部署成功
   - ❌ 红色叉号：部署失败
   - 🟡 黄色圆圈：正在运行
4. 点击具体的运行记录可查看详细日志

#### GitHub 完整配置步骤

##### 第一步：创建 GitHub 仓库（如果还没有）

1. 登录 [GitHub](https://github.com)
2. 点击右上角 **+** → **New repository**
3. 填写仓库名称（如 `my-blog` 或 `username.github.io`）
4. 选择 **Public**（GitHub Pages 免费版需要公开仓库）
5. 点击 **Create repository**

##### 第二步：推送代码到 GitHub

```bash
# 如果是新仓库，先初始化并关联远程仓库
git init
git remote add origin https://github.com/你的用户名/仓库名.git

# 推送代码
git add .
git commit -m "初始化博客"
git push -u origin master
```

##### 第三步：配置 Actions 权限（重要！）

1. 进入仓库页面，点击 **Settings**（设置）
2. 左侧菜单找到 **Actions** → **General**
3. 滚动到底部 **Workflow permissions** 部分
4. 选择 **Read and write permissions** ✅
5. 勾选 **Allow GitHub Actions to create and approve pull requests**（可选）
6. 点击 **Save** 保存

```
Settings → Actions → General → Workflow permissions
                                    ↓
                    ☑ Read and write permissions  ← 选择这个！
                    ☐ Read repository contents only
```

##### 第四步：等待首次部署

1. 推送代码后，进入仓库的 **Actions** 标签页
2. 你会看到一个正在运行的工作流（黄色圆圈）
3. 等待几分钟，变成绿色勾号表示部署成功
4. 如果失败（红色叉号），点击查看错误日志

##### 第五步：配置 GitHub Pages

1. 进入仓库 **Settings** → **Pages**
2. **Source** 选择 `Deploy from a branch`
3. **Branch** 选择 `gh-pages` 分支，目录选择 `/ (root)`
4. 点击 **Save** 保存
5. 等待几分钟，页面顶部会显示你的网站地址

```
Settings → Pages → Build and deployment
                        ↓
    Source: Deploy from a branch
    Branch: gh-pages  /  / (root)
                        ↓
                    [Save]
```

##### 第六步：访问你的网站

部署成功后，你的博客地址为：
- 普通仓库：`https://你的用户名.github.io/仓库名/`
- 用户主页仓库（仓库名为 `username.github.io`）：`https://你的用户名.github.io/`

#### 配置检查清单

| 步骤 | 检查项 | 状态 |
|------|--------|------|
| 1 | 仓库已创建且为 Public | ☐ |
| 2 | 代码已推送到 master 分支 | ☐ |
| 3 | `.github/workflows/deploy.yml` 文件存在 | ☐ |
| 4 | Actions 权限设置为 Read and write | ☐ |
| 5 | Actions 工作流运行成功（绿色勾号） | ☐ |
| 6 | Pages 设置为 gh-pages 分支 | ☐ |
| 7 | 网站可以正常访问 | ☐ |

#### GitHub Actions 常见问题

**Q: 工作流没有触发？**
- ✅ 检查文件是否在 `.github/workflows/` 目录下
- ✅ 检查分支名是否正确（master 或 main）
- ✅ 检查 YAML 语法是否正确

**Q: 部署失败？**
- 在 Actions 页面查看错误日志
- 常见原因：依赖安装失败、构建错误、权限问题

**Q: 如何手动触发部署？**
- 进入 Actions 页面 → 选择工作流 → 点击 "Run workflow"

**Q: GITHUB_TOKEN 权限不足？**
- 进入 Settings → Actions → General
- 在 "Workflow permissions" 中选择 "Read and write permissions"

**Q: 需要配置账号密码吗？**
- ❌ **不需要**！`GITHUB_TOKEN` 是 GitHub Actions 自动提供的临时令牌
- 它在每次工作流运行时自动生成，无需手动配置
- 只要部署到同一仓库的分支（如 gh-pages），就有足够的权限
- 令牌在工作流结束后自动失效，安全可靠

---

### 方式二：手动部署

1. **配置部署信息**

   编辑 `_config.yml` 文件，配置 deploy 部分：

   ```yaml
   deploy:
     type: git
     repo: https://github.com/用户名/仓库名.git
     branch: gh-pages
   ```

2. **安装部署插件**

   ```bash
   pnpm add hexo-deployer-git
   ```

3. **执行部署**

   ```bash
   # 清理 -> 生成 -> 部署
   hexo clean && hexo generate && hexo deploy

   # 或使用简写
   hexo clean && hexo g && hexo d
   ```

---

## 常用命令速查

| 命令 | 说明 |
|------|------|
| `pnpm server` 或 `hexo s` | 启动本地服务器 |
| `pnpm build` 或 `hexo g` | 生成静态文件 |
| `pnpm clean` 或 `hexo clean` | 清理缓存和生成的文件 |
| `pnpm deploy` 或 `hexo d` | 部署到远程服务器 |
| `hexo new "标题"` | 创建新文章 |
| `hexo new page "页面"` | 创建新页面 |
| `hexo new draft "草稿"` | 创建草稿 |
| `hexo publish "草稿"` | 发布草稿 |

### 组合命令

```bash
# 清理并重新生成
hexo clean && hexo g

# 清理、生成并启动服务器
hexo clean && hexo g && hexo s

# 一键部署
hexo clean && hexo g && hexo d
```

---

## 主题配置

本博客使用 **Redefine** 主题，主题配置文件位于：

- 主题配置：`_config.redefine.yml`
- 主题目录：`themes/redefine/`

更多主题配置请参考 [Redefine 主题文档](https://redefine-docs.ohevan.com/)。

---

## 常见问题

### 1. 端口被占用

```bash
# 使用其他端口启动
hexo s -p 5000
```

### 2. 页面显示异常

```bash
# 清理缓存后重新生成
hexo clean && hexo g && hexo s
```

### 3. 部署失败

- 检查 `_config.yml` 中的 deploy 配置
- 确保已安装 `hexo-deployer-git` 插件
- 检查 Git 仓库权限

---

## 相关链接

- [Hexo 官方文档](https://hexo.io/zh-cn/docs/)
- [Redefine 主题文档](https://redefine-docs.ohevan.com/)
- [Markdown 语法指南](https://www.markdownguide.org/)
